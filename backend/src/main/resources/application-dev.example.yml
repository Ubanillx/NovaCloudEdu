# =================================================================
# NovaCloudEdu — 开发环境覆盖
# 所有三方外部 API 密钥统一从 .env 文件或系统环境变量读取
# 不在此文件中硬编码任何密钥！
# 启动前请确保 docker/.env 已正确配置，或设置对应的系统环境变量
# =================================================================

# ---- 日志：开发环境 DEBUG ----
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.novacloudedu: DEBUG
    org.mybatis: DEBUG
