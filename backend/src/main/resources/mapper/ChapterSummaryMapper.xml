<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novacloudedu.backend.infrastructure.persistence.mybatis.mapper.ChapterSummaryMapper">

    <select id="findByChapterIdAndType" resultType="com.novacloudedu.backend.infrastructure.persistence.po.ChapterSummaryPO">
        SELECT * FROM chapter_summary
        WHERE chapter_id = #{chapterId} AND summary_type = #{summaryType} AND is_delete = 0
        LIMIT 1
    </select>

    <select id="findByChapterId" resultType="com.novacloudedu.backend.infrastructure.persistence.po.ChapterSummaryPO">
        SELECT * FROM chapter_summary
        WHERE chapter_id = #{chapterId} AND is_delete = 0
        ORDER BY create_time DESC
    </select>

    <select id="countByChapterIdAndType" resultType="int">
        SELECT COUNT(*) FROM chapter_summary
        WHERE chapter_id = #{chapterId} AND summary_type = #{summaryType} AND is_delete = 0
    </select>

</mapper>
