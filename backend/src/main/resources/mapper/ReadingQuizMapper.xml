<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novacloudedu.backend.infrastructure.persistence.mapper.ReadingQuizMapper">

    <select id="findByChapterId" resultType="com.novacloudedu.backend.infrastructure.persistence.po.ReadingQuizPO">
        SELECT * FROM reading_quiz
        WHERE chapter_id = #{chapterId} AND is_delete = 0
        ORDER BY create_time DESC
    </select>

    <select id="findLatestByChapterId" resultType="com.novacloudedu.backend.infrastructure.persistence.po.ReadingQuizPO">
        SELECT * FROM reading_quiz
        WHERE chapter_id = #{chapterId} AND is_delete = 0
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <delete id="deleteByChapterId">
        UPDATE reading_quiz
        SET is_delete = 1
        WHERE chapter_id = #{chapterId}
    </delete>

</mapper>
