<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novacloudedu.backend.infrastructure.persistence.mapper.AiConversationMapper">

    <select id="findByUserId" resultType="com.novacloudedu.backend.infrastructure.persistence.po.AiConversationPO">
        SELECT * FROM ai_conversation
        WHERE user_id = #{userId} AND is_delete = 0
        ORDER BY update_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByUserIdAndBookId" resultType="com.novacloudedu.backend.infrastructure.persistence.po.AiConversationPO">
        SELECT * FROM ai_conversation
        WHERE user_id = #{userId} AND book_id = #{bookId} AND is_delete = 0
        ORDER BY update_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByUserIdAndType" resultType="com.novacloudedu.backend.infrastructure.persistence.po.AiConversationPO">
        SELECT * FROM ai_conversation
        WHERE user_id = #{userId} AND conversation_type = #{conversationType} AND is_delete = 0
        ORDER BY update_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>
